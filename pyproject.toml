[tool.uv.workspace]
members = ["src/acac_charpente_vectoriser", "src/abac_charpente", "src/sapeg_regen_stock", "src/abac_visuel"]

[tool.uv.sources]
sapeg-regen-stock = { workspace = true }
abac-charpente = { workspace = true }
abac-charpente-vectoriser = { workspace = true }

[dependency-groups]
dev = [
    "abac-charpente[dev]",
    "duckdb>=1.5.1",
    "pykernel>=0.1.6",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: tests de performance (exclure avec -m 'not slow')",
]

[tool.coverage.run]
source = ["src"]
